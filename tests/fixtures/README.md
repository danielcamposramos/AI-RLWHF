# Test Fixtures

Minimal sample payloads reused across automated tests. Keep fixtures anonymized and small.

- `offline_reference.jsonl`: Offline prompt→reference pairs referenced by slot-aware runners when `enable_offline_validation` is active in tests.
